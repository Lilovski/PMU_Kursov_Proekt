<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:background="@drawable/gradient_bg"
    android:padding="20dp"
    android:gravity="center_horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/progressText"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:layout_marginBottom="6dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/timerText"
        android:textColor="#FFEB3B"
        android:textSize="14sp"
        android:layout_marginBottom="12dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="6dp">

        <TextView
            android:id="@+id/questionText"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="#222"
            android:padding="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </androidx.cardview.widget.CardView>

    <Button android:id="@+id/answer1" style="@style/quizButton"/>
    <Button android:id="@+id/answer2" style="@style/quizButton"/>
    <Button android:id="@+id/answer3" style="@style/quizButton"/>
    <Button android:id="@+id/answer4" style="@style/quizButton"/>

</LinearLayout>